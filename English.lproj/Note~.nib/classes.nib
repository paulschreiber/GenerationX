{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {handleCancel = id; handleOk = id; }; 
            CLASS = NoteController; 
            LANGUAGE = ObjC; 
            OUTLETS = {header_text = id; note_text = id; note_window = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}